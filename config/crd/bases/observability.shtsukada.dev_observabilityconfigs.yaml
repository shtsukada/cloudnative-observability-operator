---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: observabilityconfigs.observability.shtsukada.dev
spec:
  group: observability.shtsukada.dev
  names:
    kind: ObservabilityConfig
    listKind: ObservabilityConfigList
    plural: observabilityconfigs
    singular: observabilityconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ObservabilityConfig
            properties:
              endpoint:
                description: 'Example field: endpoint URL of OTLP exporter'
                maxLength: 2048
                pattern: ^https?://
                type: string
              metricsEnabled:
                default: true
                description: Enable metrics pipeline
                type: boolean
              samplingPercent:
                default: 10
                description: 'Example: sampling ratio percent (0-100)'
                format: int32
                maximum: 100
                minimum: 0
                type: integer
            required:
            - endpoint
            type: object
          status:
            description: status defines the observed state of ObservabilityConfig
            properties:
              phase:
                default: Reconciling
                enum:
                - Ready
                - Error
                - Reconciling
                type: string
              reason:
                description: Reason for last transition
                maxLength: 1024
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
